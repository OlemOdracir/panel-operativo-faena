[project]
name = "faena-reading-generator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pandas>=2.2,<3", "requests>=2.32,<3"]

[project.optional-dependencies]
test = ["pytest>=8.3,<9"]

[project.scripts]
faena-reading-generator = "generator:main"

[tool.setuptools]
py-modules = ["generator"]
